package pyramid_course_assignments.section7.challenge_problems;

import java.util.ArrayList;

/*
    PROBLEM:
        You have been given an array containing numbers.
        Find and return the largest sum in a contiguous subarray within the input array.

    EXAMPLE:
        maxSubArray([1, 2, 3, -4, 6]) => 8
        The largest sum is 8, which is the sum of all elements of the array.

        maxSubArray([1, 2, -5, -4, 1, 6]) => 7
        The largest sum is 7, which is the sum of the last two elements of the array.
*/


public class MaxSubArray {
    public static int maxSubArray(ArrayList<Integer> nums) {
        // ↓↓↓↓ your code goes here ↓↓↓↓
        return 0;
    }
}
